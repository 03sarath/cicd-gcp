steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/$REPO_NAME-staging:$COMMIT_SHA', '.']
  
  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/$REPO_NAME-staging:$COMMIT_SHA']
  
  # Deploy container image to Cloud Run (Staging)
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - '$REPO_NAME-staging'
      - '--image'
      - 'gcr.io/$PROJECT_ID/$REPO_NAME-staging:$COMMIT_SHA'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--port'
      - '8080'
      - '--set-env-vars'
      - 'ENVIRONMENT=staging'

# Store images in Google Container Registry
images:
  - 'gcr.io/$PROJECT_ID/$REPO_NAME-staging:$COMMIT_SHA'

# Optional: Add timeout for the build
timeout: '1200s' 